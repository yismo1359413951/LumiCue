# 靓相 Shotlit · 每日复盘

## 2026-06-23（项目第一天：从零搭起）

### 📋 今天做了什么（标验证级别）
- ✅ **环境硬关攻克**：清磁盘 → 发现 Xcode 16.4 装不了(系统版本) → 下 16.4 又发现 Snapzy 要 Swift6.2 → 换 Xcode 26.3 → 装 Metal 工具链 → 基座 Snapzy 编译+运行（界面验证）
- ✅ **露脸摄像头 bubble**：圆形浮窗 + 实时画面 + 可拖动（界面验证）
- ✅ **异形框 8 形状**：圆/方/圆角/椭圆/心❤️/星/六边/三角 + 单击切换（界面验证）
- ✅ **Metal 双边滤波磨皮 + 美白**（界面验证，保边不糊）
- ✅ **9 个 3D LUT 滤镜**：电影/日系/奶油肌/胶片等（界面验证）
- ✅ **人脸跟随**：Vision 检测 + 缩小 40% 居中 + 防抖 + 模糊背景填充消黑边（界面验证）
- ✅ **水平镜像**（消左右反别扭，界面验证）
- ✅ **整脸瘦脸**：Metal 液化 + 限人脸区(背景不变形)（界面验证）
- ✅ **隐形提词器**：sharingType=.none 录屏/直播隐形（ScreenCaptureKit 对照铁证）+ 右键调速度/字号/大小/暂停 + 自动滚动（界面验证）
- ✅ **单击选项菜单**（形状/滤镜/美颜强度）
- ⚠️ **挖出 2 个超值方向**：虚拟摄像头(直播/会议通用)、直播隐形提词器

### 👍 做得好的 + 怎么优化
1. **每次撞坑当场记教训进 memory**（今天 4 条：工具链版本、美颜要专业 shader、调研要用户走查、验证隐形用真捕获）——优化：明天开工先读这 4 条，别重犯。
2. **环境关没退缩**：8 次编译排障、换 Xcode、装组件，硬啃下来——优化：以后二开先 grep 项目工具链版本，省这一圈。
3. **隐形提词器最后用对照实验真验证**（可见 vs 隐形都用 ScreenCaptureKit），洗刷了之前误报。

### 👎 今天我犯的错（诚实，一条不藏）
1. 🔴🔴 **隐形提词器误报成功**（最严重）：用 `screencapture` 验证隐形(它是特例、不遵守 sharingType)，还看漏了证据(把对话里旧截图当真提词器)，就报"隐形成功"——违反"done 必带铁证"。被你当场抓。后用 ScreenCaptureKit 对照修正。教训已记 [[verify-invisibility-with-real-capture]]。
2. 🔴 **选 Xcode 16.4 没先查工具链版本** → 撞满盘 Swift6.2 隔离错 → 8 次编译排障才换 26.3。教训已记 [[confirm-toolchain-version-before-fork]]。
3. 🔴 **美颜用 Core Image 凑合** → 磨皮"雾蒙蒙"翻车 2 次 → 才改 Metal 双边。教训已记 [[beauty-needs-real-gpu-shader]]。
4. 滤镜用 colorControls 调参数(不好看) → 改 3D LUT。
5. 瘦脸液化没限人脸区 → 背景变形 → 加 faceMask 修。
6. 下巴瘦脸用脸框近似(不准、奇怪) → 关掉，待 Vision 精确关键点。
7. **反复用"给选项 + 建议明天"结尾**，你烦到问"你要睡觉吗"——我是 AI 不累，该自己决断、直接做，不推诿。
8. 🔴 **提词器调框大小后文字被切**（你收工时抓到）：setBoxSize 没重新布局 textView。根因=**我只验"默认大小"、没验"调节后状态"**——做了调节功能却不测调节后的样子。明天先修。
> 共性：**赶工手搓精细效果必翻 + 验证覆盖不全**（美颜/滤镜/瘦脸/隐形/调框都栽在"没用对方法验"或"没验边界/调节后状态"）。提升：效果类功能先用对算法/对验证；做了"可调/可变"的功能必须测"调节后/边界"的样子，不只默认态。

### 🎯 明天要做（优先级 + 具体）
1. 🔴 **Vision 精确分部位美颜**（你定的手搓路线）：先吃透 Apple Vision 76 点关键点，逐部位做——瘦下巴/瘦颧骨/瘦鼻/大眼。先做下巴(今天脸框近似失败的)。
2. 🔴 **隐形提词器补验**：确认你屏幕本地能看到(今天没让你最终确认)+ 用真录屏/OBS 实测一遍隐形。
3. 🟡 **提词器完善**：导入 txt 稿 / 粘贴稿持久化。
4. 🟡 **录制集成**：bubble + 提词器接进基座录屏流程(bubble 录进去、提词器排除)。
5. 🟡 **虚拟摄像头**(CoreMediaIO)：让美颜能被任何直播/会议软件选为摄像头源。
6. ⚪ 高清录制配置 / 竖屏 / 中文字幕 / 自动 zoom。

### 📌 一句话总结
**项目第一天从零起步**：啃下环境硬关 + 做出露脸/异形框/磨皮美白/9滤镜/人脸跟随/镜像/整脸瘦脸/隐形提词器一大批(全界面验证)；代价是赶工反复翻车(美颜雾蒙、瘦脸背景变形、隐形一度误报)，但每个错都当场修+记教训，隐形提词器最终带对照铁证拿下。
